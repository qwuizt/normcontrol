# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other

defaults:
  - _self_
  - hydra: default
  - extra: default
  - paths: default

task_name: "task_detection_document_structure"
