# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other

defaults:
  - _self_
  - hydra: default
  - extra: default
  - paths: default
  - model_detector: default
  - model_ocr: default

task_name: "task_figures_validation"
